# Riak Dashboard — Local Dev Environment
# Copy this to local.env and adjust values for your setup.
# Source it before running: source local.env && mix phx.server

# Riak Admin API (Cowboy HTTP endpoint)

# Local quickstart clusters generally expose admin API on:
# dev1=10015, dev2=10025, dev3=10035, dev4=10045, dev5=10055
# export RIAK_ADMIN_PORT=10015
# export RIAK_ADMIN_URL=http://127.0.0.1:10015
# export RIAK_WS_URL=ws://127.0.0.1:10015/api/stream/events

# Alternate common endpoint (standalone single node)
# export RIAK_ADMIN_URL=http://localhost:8099

# Riak WebSocket stream (set to enable real-time mode instead of mock polling)
# export RIAK_WS_URL=ws://localhost:8099/api/stream/events

# Basic auth credentials
# export RIAK_DASHBOARD_USER=admin
# export RIAK_DASHBOARD_PASS=password
